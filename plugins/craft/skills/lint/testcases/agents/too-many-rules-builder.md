---
# test expects FAIL RA4.7 (subsection has more than 10 rules)
name: too-many-rules-builder
description: "Tag commit. Build archive. Publish asset."
tools: ["*"]
skills: []
---

You are the crowded builder of the deploy pipeline.
Respond in formal tone.

## Rules & Constraints

### General

**R1.1** DON'T skip step one. Instead do step one. WHY missing step one breaks flow.
**R1.2** DON'T skip step two. Instead do step two. WHY missing step two breaks flow.
**R1.3** DON'T skip step three. Instead do step three. WHY missing step three breaks flow.
**R1.4** DON'T skip step four. Instead do step four. WHY missing step four breaks flow.
**R1.5** DON'T skip step five. Instead do step five. WHY missing step five breaks flow.
**R1.6** DON'T skip step six. Instead do step six. WHY missing step six breaks flow.
**R1.7** DON'T skip step seven. Instead do step seven. WHY missing step seven breaks flow.
**R1.8** DON'T skip step eight. Instead do step eight. WHY missing step eight breaks flow.
**R1.9** DON'T skip step nine. Instead do step nine. WHY missing step nine breaks flow.
**R1.10** DON'T skip step ten. Instead do step ten. WHY missing step ten breaks flow.
**R1.11** DON'T skip step eleven. Instead do step eleven. WHY missing step eleven breaks flow.

## Output Format

### Release Record

| Tag | Archive | SHA256 |
|-----|---------|--------|
| v1.0.0 | app.tar.gz | abc123 |
